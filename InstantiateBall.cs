using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class InstantiateBall : MonoBehaviour
{
    public GameObject Ball;

    // Start is called before the first frame update
    void Start()
    {
        Instantiate(Ball, new Vector3(1.192f, 0.9f, 2.865f), Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        //GameObject ball = Instantiate(Ball, new Vector3(1.192f, 0.9f, 2.865f), Quaternion.identity);
        //sball.position += transform.right * (Time.deltaTime * 4);
    }
}*/
